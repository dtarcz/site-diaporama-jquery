$(document).ready(function(){
    // on exécute la fonction diaporama sur tous les <ul> qui ont une classe .diaporama
    $("ul.diaporama").diaporama({
        animationSpeed: "slow",
        delay:2
    });

});
